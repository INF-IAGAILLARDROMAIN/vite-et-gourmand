# Database (PostgreSQL - Neon recommended)
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# MongoDB (Atlas recommended - for admin stats / NoSQL)
MONGODB_URI="mongodb+srv://user:password@cluster.mongodb.net/vite-et-gourmand?retryWrites=true&w=majority"

# JWT
JWT_SECRET="change-me-in-production-use-a-long-random-string"
JWT_EXPIRES_IN="7d"

# Frontend URL (for CORS and email links)
FRONTEND_URL="http://localhost:3001"

# SMTP (use Ethereal for dev: https://ethereal.email)
SMTP_HOST="smtp.ethereal.email"
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
MAIL_FROM="Vite & Gourmand <noreply@viteetgourmand.fr>"
